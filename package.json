{
  "name": "bearer-protocol-elections-unified-edition",
  "version": "0.1.0",
  "private": true,
  "description": "BEARER Protocol Elections Unified Edition conceptual browser demo.",
  "type": "module",
  "scripts": {
    "validate": "node ./scripts/validate-demo.js",
    "test": "node --test",
    "check": "npm run validate && npm test"
  }
}
