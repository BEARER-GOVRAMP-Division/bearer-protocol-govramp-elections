{
  "name": "bearer-protocol-govramp-elections",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node ./scripts/validate-demo.js",
    "test": "node --test",
    "check": "npm run validate && npm test"
  }
}
